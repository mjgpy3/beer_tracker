<!DOCTYPE html>
<html>
  <head>
    <title>BeerTracker</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body style="margin-right:10%; margin-left:10%;">

  <div class="banner">
    <h2>Beer Tracker (Beta)</h2>
    <p>"For the beer connoisseur"</p>
    <table style="width:100%;border: 1px solid black;">
      <tr>
        <td><%= link_to "My Profile", profile_index_path %></td>
        <td><%= link_to "View Beers", beers_path %></td>
        <td><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></td>
      <tr>
    </table>
  </div>

  <% if params[:controller].include? "devise"%>
  <p class="notice" style="color:green;"><%= notice %></p>
  <p class="alert" style="color:red;"><%= alert %></p>
  <% end %>

  <%= yield %>

  </body>
</html>
